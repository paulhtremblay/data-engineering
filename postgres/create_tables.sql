CREATE TABLE test_table (
    field1      varchar(40) NOT NULL,
    field2      varchar(40) NOT NULL
);

CREATE TABLE test_enrichement (
    field1      varchar(40) NOT NULL,
    field2      varchar(40) NOT NULL
);
